pipeline {
    agent { label 'shuup' }

    stages {
        stage('test'){
            steps{
                echo "code is successfully test"
            }
        }




        stage('build'){
            steps{
                echo 'docker is success fully connected'
            }
        }
    }
}
